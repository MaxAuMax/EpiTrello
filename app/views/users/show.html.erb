<%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "logout-button" %>

<div class="user-dashboard">
    <div>
        <h1>Bienvenue, <%= @user.username %> 👋</h1>
        <p style="text-align: center;">Voici vos projets :</p>
    </div>

    <ul class="projects-list">
        <% @user.projects.each do |project| %>
            <li class="project-card">
                <h2><%= project&.name %></h2>
                <p><span style="font-weight: bold;">Description :</span><%= project&.description %></p>
                <p><span style="font-weight: bold;">Statut :</span> <%= project&.project_status&.name %></p>
                <%= link_to "Voir le projet", project_path(project) %>
            </li>
        <% end %>

        <li class="project-card project-card-add">
            <%= link_to new_project_path, class: "add-project-link" do %>
                <div class="add-project-content">
                    <span class="plus-icon">+</span>
                    <p>Nouveau projet</p>
                </div>
            <% end %>
        </li>
    </ul>
</div>

<style>

.logout-button {
    position: absolute;
    top: 10px;
    right: 10px;
    padding: 1rem;
    border-radius: 0.5rem;
    width: auto;
    background-color: #004db3ff;
    color: white;
    font-weight: bold;
}

.user-dashboard {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
}

.projects-list {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    padding: 0;
    list-style-type: none;
}

.project-card {
    border: 1px solid #ccc;
    padding: 1rem;
    border-radius: 0.5rem;
    width: 300px;
}

.project-card-add {
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #f5f5f5;
    border: 2px dashed #ccc;
    cursor: pointer;
    transition: all 0.3s ease;
}

.project-card-add:hover {
    border-color: #004db3ff;
    background-color: #e8f0ff;
}

.add-project-link {
    text-decoration: none;
    color: #004db3ff;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
}

.add-project-content {
    text-align: center;
}

.plus-icon {
    font-size: 3rem;
    font-weight: bold;
    display: block;
    margin-bottom: 0.5rem;
}

.add-project-content p {
    margin: 0;
    font-weight: bold;
}
</style>
