<div data-modal-target="modal" class="task-modal-wrapper hidden">
  <div class="task-modal-backdrop" data-action="click->modal#close">
    <div class="task-modal-content" onclick="event.stopPropagation()">
      <div class="task-modal-header-wrapper">
        <h2 class="task-modal-title-main"><%= title %></h2>
        <button type="button" class="modal-close-button" data-action="click->modal#close" aria-label="Close">
          <svg style="width: 18px; height: 18px;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="18" y1="6" x2="6" y2="18"></line>
            <line x1="6" y1="6" x2="18" y2="18"></line>
          </svg>
        </button>
      </div>

      <div class="task-modal-body-wrapper">
        <%= yield %>
      </div>
    </div>
  </div>
</div>

<style>
.task-modal-wrapper {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  display: none;
}

.task-modal-wrapper:not(.hidden) {
  display: block;
}

.task-modal-backdrop {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(4px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-xl);
  overflow-y: auto;
}

.task-modal-content {
  background: white;
  border-radius: var(--radius-xl);
  width: 90%;
  max-width: 500px;
  margin: auto;
  box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  position: relative;
  animation: modalSlideIn 0.2s ease-out;
}

@keyframes modalSlideIn {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.task-modal-header-wrapper {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-xl);
  border-bottom: 1px solid var(--color-slate-200);
}

.task-modal-title-main {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--color-slate-900);
  margin: 0;
}

.modal-close-button {
  width: 32px;
  height: 32px;
  border-radius: var(--radius-md);
  background: var(--color-slate-100);
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all var(--transition-base);
  color: var(--color-slate-600);
}

.modal-close-button:hover {
  background: var(--color-slate-200);
}

.task-modal-body-wrapper {
  padding: var(--spacing-xl);
  max-height: 60vh;
  overflow-y: auto;
}
</style>
